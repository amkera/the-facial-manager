<h1>Edit Esthetician Profile</h1>

<% if @esthetician.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@esthetician.errors.count, "error") %>
      prohibited this post from being saved:
    </h2>

    <ul>
    <% @esthetician.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@esthetician) do |f| %>
  <label>E-mail Address::</label> <br>
  <%= f.text_field :email %><br>
  <br />

  <label>Password:</label> <br>
  <%= f.password_field :password %><br>

  <%= f.submit %>

<% end %>
